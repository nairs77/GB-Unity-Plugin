using System;

namespace Joycity
{
	using Joycity.Callback;

	internal interface IPermissionHelper
	{
		
		void SetPermissionCallback (JoypleDelegate<JoyplePermissionResult> callback);

		void SetAutoOpenPermissionView (bool enabled);

		bool IsAutoOpenPermissionView ();

		bool IsPermissionGranted (string permission);

		bool IsPermissionGranted (string[] permissions);

		bool ShouldShowRequestPermissionRationale (string permission);

		void RequestPermission (string permission, JoypleDelegate<JoyplePermissionResult> callback);

		void RequestPermission (string[] permissions, JoypleDelegate<JoyplePermissionResult> callback);

		void ShowDetailPermissionView(bool isSnack, string permission, JoypleDelegate<JoyplePermissionResult> callback);

		void ShowDetailPermissionView(bool isSnack, string[] permissions, JoypleDelegate<JoyplePermissionResult> callback);

		void ShowPermissionSnack (string permission, JoypleDelegate<JoyplePermissionResult> callback);

		void ShowPermissionSnack (string permission, long lDuration, JoypleDelegate<JoyplePermissionResult> callback);

		void ShowPermissionSnack (string permission, SnackbarDuration duration, JoypleDelegate<JoyplePermissionResult> callback);

	}
}

